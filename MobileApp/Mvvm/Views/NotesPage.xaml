<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Northboundei.Mobile.Mvvm.Views.NotesPage"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:sections="clr-namespace:Northboundei.Mobile.Mvvm.Views.Sections"
             xmlns:vms="clr-namespace:Northboundei.Mobile.Mvvm.ViewModels"
             NavigationPage.HasNavigationBar="False"
             NavigationPage.HasBackButton="False"
             x:DataType="vms:NotesViewModel"
             Title="">
    <Grid RowDefinitions="Auto,*,Auto">
        <!-- Header, Saving functions-->
        <Border Grid.Row="0">
            <Grid Margin="0,15" ColumnDefinitions="*,*,*">
                <Button Text="Cancel" Command="{Binding CancelCommand}" HorizontalOptions="Start" />
                <Button Text="Save As Draft" Grid.Column="1" Command="{Binding SaveAsDraftCommand}" HorizontalOptions="Center" />
                <Button Text="Save" Grid.Column="2" Command="{Binding SaveCommand}" HorizontalOptions="End" />
            </Grid>
        </Border>

        <ContentView Grid.Row="1" Content="{Binding CurrentPage}"/>
        
        <!-- Footer, Navigation functions-->
        <Border Grid.Row="2">
            <Grid Margin="0,15" ColumnDefinitions="*,*">
                <Button Text="Previous Section" Command="{Binding PreviousSectionCommand}" HorizontalOptions="Start" />
                <Button Text="Next Section" Command="{Binding NextSectionCommand}" HorizontalOptions="End" />
            </Grid>
        </Border>
    </Grid>
</ContentPage>
