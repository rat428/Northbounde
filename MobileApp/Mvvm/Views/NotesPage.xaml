<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Northboundei.Mobile.Mvvm.Views.NotesPage"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:sections="clr-namespace:Northboundei.Mobile.Mvvm.Views.Sections"
             xmlns:vms="clr-namespace:Northboundei.Mobile.Mvvm.ViewModels"
             NavigationPage.HasNavigationBar="False"
             NavigationPage.HasBackButton="False"
             x:DataType="vms:NotesViewModel"
             Title="{Binding CurrentSectionPage.BindingContext.SectionTitle}">
    <Grid RowDefinitions="*,Auto">
        <ContentView Content="{Binding CurrentSectionPage}"/>
        
        <!-- Footer, Navigation functions-->
        <Border Grid.Row="1">
            <Grid Margin="0,15" ColumnDefinitions="*,*,*">
                <Button Text="{Binding PreviousButtonText}" Command="{Binding PreviousSectionCommand}" HorizontalOptions="Center" />
                <Button Grid.Column="1" Text="Save Draft" Command="{Binding Save}" HorizontalOptions="Center" />
                <Button Grid.Column="2" Text="{Binding NextButtonText}" Command="{Binding NextSectionCommand}" HorizontalOptions="Center" />
            </Grid>
        </Border>
    </Grid>
</ContentPage>
