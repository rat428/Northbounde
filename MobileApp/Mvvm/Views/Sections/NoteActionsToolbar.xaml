<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vms="clr-namespace:Northboundei.Mobile.Mvvm.ViewModels"
             x:Class="Northboundei.Mobile.Mvvm.Views.Sections.NoteActionsToolbar"
             x:DataType="vms:HomeViewModel">
    <VerticalStackLayout Margin="0,8">
        <HorizontalStackLayout>
            <Button Text="Discard" Command="{Binding DiscardCommand}" HorizontalOptions="Start" />
            <Button Text="Draft" Command="{Binding SaveDraftCommand}" HorizontalOptions="Center" />
            <Button Text="Submit" Command="{Binding SubmitCommand}" HorizontalOptions="End" />
        </HorizontalStackLayout>
    </VerticalStackLayout>
</ContentView>